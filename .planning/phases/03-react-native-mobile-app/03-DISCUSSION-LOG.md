# Phase 3: React Native Mobile App — Discussion Log

**Date:** 2026-05-26
**Status:** Complete

---

## Area 1: Repository & Code Sharing

| Question | Options presented | Selection |
|---|---|---|
| Where should the RN app live? | Monorepo (Turborepo) / Same repo sibling folder / Separate repo | **Monorepo (Turborepo)** |
| What goes in packages/shared? | Types+API+utils / Types+API+utils+Zod / Types only | **Types + API client + utils + Zod schemas** |
| RN foundation? | Expo managed / Expo bare / Bare RN | **Expo (managed workflow)** |

**Decision:** Turborepo `/apps/web` + `/apps/mobile` + `/packages/shared`. Expo managed workflow with expo-router.

---

## Area 2: Feature Scope for Mobile v1

| Question | Options presented | Selection |
|---|---|---|
| Which screens in v1? | Core-first / Full parity / Ultra-lean | **Full parity** |
| Navigation structure? | Tab bar / Tab bar + drawer / Stack-only | **Tab bar (5 tabs matching web)** |

**Decision:** All screens ship in v1. Tab bar: Home, Map, +Add, Pings, Profile.

---

## Area 3: Auth & API Strategy

| Question | Options presented | Selection |
|---|---|---|
| How does RN talk to backend? | Add REST/tRPC to existing server / BFF / Extend RR API routes | **tRPC on existing server** |
| API style? | tRPC / REST JSON / GraphQL | **tRPC** |
| Auth on mobile? | Better Auth Expo SDK / Custom JWT / WebView login | **Better Auth Expo SDK** |

**Decision:** tRPC mounted at `/api/trpc/*` on existing server. Better Auth Expo SDK with expo-secure-store tokens.

---

## Area 4: Real-time & Push

| Question | Options presented | Selection |
|---|---|---|
| Chat real-time? | SSE via polyfill / WebSocket / Polling | **SSE (react-native-event-source polyfill)** |
| Push notifications? | Expo Push (EAS) / Extend web-push / FCM direct | **Expo Push Notifications** |
| Payments on mobile? | WebView PayFast / IAP / Defer to web | **WebView-based PayFast (expo-web-browser)** |

**Decision:** SSE polyfill for chat (no server changes). Expo Push for native notifications (new push token endpoint needed). PayFast via expo-web-browser WebView with deep link return.

---

*Generated by gsd-discuss-phase | Phase 3*
